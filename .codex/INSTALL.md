# ASO Skills for Codex

Add these skills to your Codex agent:

```bash
cp -r skills/ ~/.codex/skills/aso/
```

Skills will be auto-discovered from SKILL.md frontmatter.

## Prerequisites

Install the ASO CLI:

```bash
brew install asomaniac/tap/aso
aso maniac login
```
